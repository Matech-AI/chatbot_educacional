# Requirements Completo - Sistema RAG + API
# Combina todas as dependências necessárias para ambos os servidores

# Core FastAPI e servidor
fastapi
uvicorn
python-multipart
python-dotenv

# LangChain e componentes RAG
langchain
langchain-openai
langchain-core
langchain-text-splitters
langchain-chroma
langchain_community

# LangGraph (versão compatível com Python 3.11)
langgraph>=0.2.20,<0.3

# Dependências específicas do LangGraph
ormsgpack==1.10.0
xxhash==3.5.0

# OpenAI e embeddings
openai
tiktoken
sentence-transformers

# Processamento de documentos
PyPDF2
pypdf
python-docx
openpyxl
docx2txt
unstructured
python-magic
python-pptx

# Normalização de documentos para Markdown (markitdown pipeline)
# Sem [all] para evitar onnxruntime pesado — áudio/OCR não são necessários aqui
markitdown>=0.1.0

# ChromaDB para vector store
chromadb

# Processamento de planilhas
pandas

# Google Drive API
google-auth-oauthlib
google-auth-httplib2
google-api-python-client

# LangChain para chat agents
langchain-google-genai
google-generativeai

# NVIDIA AI Endpoints (lib oficial LangChain)
langchain-nvidia-ai-endpoints

# Autenticação e segurança
python-jose
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Utilitários
pydantic[email]  # Adicionado suporte para validação de email
requests
aiohttp
pytz
magic-wormhole  # Para transferência segura de arquivos via wormhole

# Performance e cache (opcional)
redis
aioredis

# Supabase (se usar)
supabase

# Dependências adicionais
langsmith
sqlalchemy
psycopg2-binary  # driver Postgres (SQLite não precisa)

# Dependências específicas do LangChain (versões compatíveis)
langchain-community>=0.0.10
langchain-core>=0.1.10

# Dependências para embeddings open source
transformers
torch
torchvision
torchaudio