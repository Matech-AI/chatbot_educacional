# Requirements para o Servidor RAG
# Contém as dependências necessárias para o servidor RAG

# Core FastAPI e servidor (se o RAG server também for um FastAPI app)
fastapi
uvicorn
python-multipart
python-dotenv

# LangChain e componentes RAG
langchain
langchain-openai
langchain-core
langchain-text-splitters
langchain-chroma
langchain_community

# LangGraph (versão compatível com Python 3.11)
langgraph>=0.2.20,<0.3

# Dependências específicas do LangGraph
ormsgpack==1.10.0
xxhash==3.5.0

# OpenAI e embeddings
openai
tiktoken
sentence-transformers

# Processamento de documentos
PyPDF2
python-docx
openpyxl
docx2txt
unstructured
python-magic

# ChromaDB para vector store
chromadb

# Processamento de planilhas
pandas

# Google Drive API (se o RAG server precisar acessar o Drive diretamente)
google-auth-oauthlib
google-auth-httplib2
google-api-python-client

# LangChain para chat agents
langchain-google-genai
google-generativeai

# Autenticação e segurança (se o RAG server precisar de autenticação direta)
python-jose
passlib[bcrypt]
sqlalchemy

# Utilitários
pydantic[email]
requests
aiohttp
pytz
magic-wormhole

# Performance e cache (opcional)
redis
aioredis

# Dependências adicionais
langsmith

# Dependências específicas do LangChain (versões compatíveis)
langchain-community>=0.0.10
langchain-core>=0.1.10

# Dependências para embeddings open source (estas são as grandes)
transformers
torch
torchvision
torchaudio